---
import TurnstileLoader from "@/islands/components/TurnstileLoader.astro";
import { LoginFormIsland } from "@/islands/forms/LoginForm.tsx";
import { INVITE_ONLY } from "astro:env/server";
---

<TurnstileLoader>
  <div class="relative flex w-full items-center justify-center p-6 md:p-10">
    <div class="w-full max-w-sm bg-white">
      <LoginFormIsland client:only="react" inviteOnly={INVITE_ONLY} />
    </div>
  </div>
</TurnstileLoader>
